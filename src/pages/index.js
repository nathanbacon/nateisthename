import * as React from "react";
import Layout from "../components/layout";

// markup
const IndexPage = () => {
  return (
    <Layout>
      <p>Hello, world</p>
    </Layout>
  );
};

export default IndexPage;
